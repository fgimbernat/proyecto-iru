<% content_for :page_title, "Dashboard" %>

<div class="dashboard-container">
  <!-- Header -->
  <div class="dashboard-header">
    <h1 class="dashboard-title">Dashboard</h1>
    <p class="dashboard-subtitle">Resumen general del sistema</p>
  </div>

  <!-- Tarjetas de estadísticas -->
  <div class="dashboard-stats-grid">
    <!-- Card Usuarios -->
    <div class="dashboard-stat-card">
      <div class="stat-card-content">
        <div class="stat-icon-box stat-icon-box--blue">
          👥
        </div>
        <div class="stat-info">
          <p class="stat-label">Usuarios</p>
          <p class="stat-value"><%= @users_count %></p>
        </div>
      </div>
      <div class="stat-card-footer">
        <%= link_to admin_users_path, class: "stat-link stat-link--blue" do %>
          Ver todos
          <svg fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7" />
          </svg>
        <% end %>
      </div>
    </div>

    <!-- Card Empleados -->
    <div class="dashboard-stat-card">
      <div class="stat-card-content">
        <div class="stat-icon-box stat-icon-box--green">
          👤
        </div>
        <div class="stat-info">
          <p class="stat-label">Empleados</p>
          <p class="stat-value"><%= @employees_count %></p>
        </div>
      </div>
      <div class="stat-card-footer">
        <a href="#" class="stat-link stat-link--green">
          Ver todos
          <svg fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7" />
          </svg>
        </a>
      </div>
    </div>

    <!-- Card Departamentos -->
    <div class="dashboard-stat-card">
      <div class="stat-card-content">
        <div class="stat-icon-box stat-icon-box--purple">
          🏢
        </div>
        <div class="stat-info">
          <p class="stat-label">Departamentos</p>
          <p class="stat-value"><%= @departments_count %></p>
        </div>
      </div>
      <div class="stat-card-footer">
        <a href="#" class="stat-link stat-link--purple">
          Ver todos
          <svg fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7" />
          </svg>
        </a>
      </div>
    </div>

    <!-- Card Puestos -->
    <div class="dashboard-stat-card">
      <div class="stat-card-content">
        <div class="stat-icon-box stat-icon-box--orange">
          💼
        </div>
        <div class="stat-info">
          <p class="stat-label">Puestos</p>
          <p class="stat-value"><%= @positions_count %></p>
        </div>
      </div>
      <div class="stat-card-footer">
        <a href="#" class="stat-link stat-link--orange">
          Ver todos
          <svg fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7" />
          </svg>
        </a>
      </div>
    </div>
  </div>

  <!-- Mensaje de bienvenida -->
  <div class="dashboard-welcome-card">
    <div class="welcome-icon-box">
      <svg fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z" />
      </svg>
    </div>
    <h2 class="welcome-title">Bienvenido al Sistema de Gestión de RRHH</h2>
    <p class="welcome-description">
      Desde aquí puedes administrar usuarios, empleados, departamentos y puestos de trabajo de manera eficiente y organizada.
    </p>
    <div class="welcome-actions">
      <%= render ButtonComponent.new(variant: :primary, size: :md, href: new_admin_user_path) do %>
        <span class="mr-2">👤</span>
        <span>Crear usuario</span>
      <% end %>
      <%= render ButtonComponent.new(variant: :outline, size: :md, href: "#") do %>
        <span class="mr-2">📚</span>
        <span>Ver documentación</span>
      <% end %>
    </div>
  </div>
</div>
